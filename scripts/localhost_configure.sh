#!/bin/bash
# This script will set items on the ansible host.
# Until I can figure out how to repalce this with a playbook.
#export ANSIBLE_HOSTS="assets/ansible.hosts"
export ANSIBLE_INVENTORY="assets/ansible.hosts"
